package org.unidocCli.test;

import org.unidocCli.annotations.EnumDoc;

@EnumDoc(description = "An enum that does what it needs to do by doing what it was created to do." +
        "how many coders would like to use this introduction? I really don't know at the moment.",
        author = "divad",
        version = "1.0",
        since = "2020",
        see = "no_other_enum")
public enum TestEnum {
}